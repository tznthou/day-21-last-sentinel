# Last Sentinel Environment Variables

# Server
PORT=3000
NODE_ENV=development

# ============================================
# ⚠️  PRODUCTION 部署必要設定 (Zeabur/Vercel/Railway 等)
# ============================================
#
# 1. ALLOWED_ORIGINS - CORS 白名單（極重要！）
#    必須設定為你的部署網域，否則前端會收到 500 錯誤
#    例如：ALLOWED_ORIGINS=https://your-app.zeabur.app
#    多個網域用逗號分隔：https://domain1.com,https://domain2.com
#
# 2. SESSION_SECRET - Session 加密金鑰
#    Production 必須設定，否則每次重啟 session 會失效
#    建議使用：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
ALLOWED_ORIGINS=https://your-deployed-domain.com
SESSION_SECRET=generate-a-random-string-here

# InsForge Configuration
INSFORGE_BASE_URL=https://75ae3yns.us-west.insforge.app
INSFORGE_ANON_KEY=your-anon-key-here

# Scraping Configuration
SCRAPE_INTERVAL=60000
USER_AGENT=Mozilla/5.0 (compatible; LastSentinel/1.0)
MAX_CONTENT_LENGTH=500000

# AI Configuration
AI_MODEL=openai/gpt-4o
AI_MAX_TOKENS=500
